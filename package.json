{
  "name": "annotate",
  "version": "1.0.0",
  "description": "Privacy-first, fully-local document annotation in the browser. Open PDF/DOCX/images; draw, highlight, comment; save as self-contained HTML. No build, no server, no dependencies.",
  "license": "Apache-2.0",
  "keywords": ["annotation", "pdf", "docx", "privacy", "local-first", "highlighter", "comments", "no-build"],
  "scripts": {
    "start": "python3 -m http.server 8777",
    "samples": "python3 test/make_samples.py",
    "test": "cd test && node qa.js && node qa2.js && node qa3.js"
  },
  "dependencies": {},
  "private": false
}
